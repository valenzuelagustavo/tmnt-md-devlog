# Carpeta de ROMs

Poné acá las compilaciones (.bin / .md) del port. Después registralas en `../data/builds.js` y corré `../deploy.sh`.

El archivo .bin que viene incluido es un PLACEHOLDER de ejemplo; reemplazalo por tu ROM real.
